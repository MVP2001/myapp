global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Добавляем правила алертов
rule_files:
  - /etc/prometheus/alerts.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - alertmanager:9093

scrape_configs:
